This script ensures it runs with administrative privileges, then uses the Windows 
Package Manager (winget) to upgrade all available software and drivers. It provides 
real-time progress feedback through a graphical progress bar and status messages. 
Upon completion, it notifies the user of the outcome.